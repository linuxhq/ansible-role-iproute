# {{ ansible_managed }}
# iproute_rt_scopes: {{ iproute_rt_scopes|length }}
{% if iproute_rt_scopes|length > 0 %}

{%   for key, value in iproute_rt_scopes.items()|sort|reverse %}
{{ key }}	{{ value }}
{%   endfor %}
{% endif %}
